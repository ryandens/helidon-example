/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package com.ryandens.example.app;

import com.ryandens.example.list.LinkedList;

import static com.ryandens.example.utilities.StringUtils.join;
import static com.ryandens.example.utilities.StringUtils.split;
import static com.ryandens.example.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
